[package]
name = "dfutil"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
bytesutil = {path = "../bytesutil"}
datafusion = "13.0"
bytes = "1.2.1"
thiserror = "1.0"
arrow = {version = "24.0", features = ["ipc", "ffi"]}
