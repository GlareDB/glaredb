# Basic test for external database.

statement ok
CREATE EXTERNAL DATABASE basic
	FROM postgres
	OPTIONS (
		postgres_conn = '${POSTGRES_CONN_STRING}',
	);

query I
SELECT count(*) FROM basic.public.bikeshare_stations;
----
102

# Ensure we can query into the virtual schema.

query TT
SELECT table_schema, table_name
	FROM basic.virtual_datasource_schema.tables
	WHERE table_schema = 'public' AND table_name = 'bikeshare_stations';
----
public  bikeshare_stations
