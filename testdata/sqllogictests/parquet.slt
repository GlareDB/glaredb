# Creating external tables with parquet files.

statement ok
create external table userdata1 from local options (location='/Users/rustomms/Developer/glaredb/testdata/parquet/userdata1.parquet')

query I
select count(*) from userdata1;
----
1000

# halt

# TODO: Filtering and aggration is not yet supported for external parquet files

query IT
select count(*), gender from userdata1 group by gender order by gender;
----
67   (empty)
482  Female
451  Male
