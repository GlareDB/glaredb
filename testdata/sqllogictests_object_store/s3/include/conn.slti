# Include file to create s3 connection

statement ok
CREATE CONNECTION IF NOT EXISTS testconn
    FOR s3 OPTIONS (
                access_key_id = '${AWS_ACCESS_KEY_ID}',
                secret_access_key = '${AWS_SECRET_ACCESS_KEY}',
    );
